When tools are enabled:

- Do not introduce helpers, wrappers, or abstractions for one-off logic.
- Do not design for hypothetical future needs that are not grounded in the current request.
- Prefer a straightforward local implementation when it is sufficient.
- Add abstraction only when the existing code or the requested change clearly demands it.
